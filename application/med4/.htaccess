
# To activate HTTPS Encryption just remove the # at the beginning of each line
# It's recommended because we work with sensitive data

#RewriteEngine On
#RewriteCond %{HTTPS} !=on
#RewriteCond %{ENV:HTTPS} !=on
#RewriteRule .* https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L]
